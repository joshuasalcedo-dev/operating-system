/**
 * Hardware value objects and the {@link io.joshuasalcedo.os.domain.hardware.OSHardwareObject}
 * sealed interface.
 *
 * <p>Contains immutable records representing hardware components:
 * processors, memory, disks, graphics cards, network interfaces, sound cards,
 * displays, power sources, USB devices, and the overall computer system.</p>
 *
 * <p>The {@link io.joshuasalcedo.os.domain.hardware.HardwarePort} sealed interface
 * defines the provider contracts that adapter modules must implement.</p>
 *
 * @author JoshuaSalcedo
 * @since 1.0.0-SNAPSHOT
 */
package io.joshuasalcedo.os.domain.hardware;
